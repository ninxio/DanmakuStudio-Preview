# AGENTS.md

## 每次任务先做

- 确认当前目录与 `git status --short --branch`，只在主工作区开发。
- 先读本文件和根目录 `progress.md`；只读取与当前任务直接相关的文件。
- 不默认遍历 `artifacts/`、构建目录、测试输出、Git 历史或旧目标。需要历史证据时按 `docs/DEVELOPMENT.md` 定点查询。
- 保留用户已有改动；提交前检查暂存区，只纳入当前阶段文件。

## 产品边界

- B 站视频和用户上传视频是弹幕来源与参考时间轴，Emby/本地原片是最终观看时间轴。
- 核心产物是投影到原片时间轴的修正弹幕 XML、项目编辑状态和可复核时间映射。
- 不剪切、分割、转码或重封装视频；原始 XML 不可直接修改。
- 多集合并、片头片尾、审核删减、空白和局部偏移用虚拟内容段、同步锚点、版本差异和映射规则非破坏性表达。
- 视频内容不嵌入项目文件；项目只保存媒体引用和编辑状态。

## UI 与领域规则

- 涉及用户界面前先读 `docs/ux-charter.md`。
- 顶层动线固定为：素材 → 匹配 → 编辑 → 导出。
- UI 文案使用简体中文；入口、状态和结果必须可理解，禁止假按钮。
- UI 核心交互需键盘可达并有清晰焦点样式。
- TypeScript 标识符和文件名使用英文；禁止无说明的 `any`。
- 内部时间统一使用整数毫秒。
- XML 解析、时间映射、导出和项目序列化不得依赖 React。
- 导出前重新解析验证 XML。

## 验证、记录与回退

- 修改核心逻辑后运行相关测试；涉及 UI 时完成从空项目到导出分集 XML 的北极星走查。
- 产生可运行代码、UI 或 Tauri 变更时，默认运行相关测试并重新打包 release；无法打包时说明原因。
- 阶段结束时覆盖更新 `progress.md`，只保留当前快照、验证结果、未完成项和下一步，不追加完整流水账。
- 每个可验收阶段创建 Git 提交；关键里程碑创建 `checkpoint/...` 标签。
- 日常回退依赖提交、分支和标签，不使用 worktree，也不使用破坏性的 `git reset --hard`。
- 测试日志、缓存和临时构建不进入 Git；证据只有在不可轻易重建且被结论明确引用时才保留。
