# Danmaku Studio 当前进度

更新时间：2026-07-20

当前分支：`codex/danmaku-studio-ux`

## 当前状态

Usability V2 阶段 0–6 已完成。应用在保留现有领域逻辑、项目格式、人工复核和导出安全门的前提下，形成素材 → 智能匹配 → 校准 → 导出的用户主流程。

日常开发仍使用单一主工作区；各阶段均有 Git 提交和 `checkpoint/usability-stage-...` 标签可回退。

## 智能批次匹配 V3（阶段 1–6 已完成）

分阶段方案已保存到 `docs/plans/2026-07-20-smart-batch-matching-v3.md`。

### 阶段 1 完成：长剧集精匹配准入

- 真实 2×8 诊断确认只有 E03、E06 进入精匹配的主因是 60 分钟完整剧集假设，不是素材损坏、德语音轨或任务数量。
- 60 分钟现在只决定是否采用短媒体完整缓存；不再决定一集是否有资格进入精匹配。
- 完整原片时间轴会定义精匹配查询，64–74 分钟原片不再因“双轴超过 60 分钟”被丢弃。
- 内存准入改为阶段峰值模型：参考与原片依次解码，已完成一侧只按 PCM 与细特征常驻量计费；CUDA 临时工作区只计入实际提取阶段，不再与两侧解码峰值重复相加。
- 新增长剧集、双长轴和 74 分钟 CPU/CUDA 预算回归测试；专项 Rust 测试通过。

### 阶段 2 完成：多音轨自动判断与必要时手选

- 内容指纹仍是音轨决策主体；已知语言匹配、默认轨和用户显式选择只作为受限先验。
- 参考语言为 `und` 时不会阻断，目标原片默认音轨获得轻量排序优势；第一、第二候选过近仍由既有 margin 门控转为人工复核。
- 匹配页高级区可真实探测所选文件的非解说音轨，显示语言、标题、默认标记和声道数，并把用户指定的 stream index 送入原生批次。
- 原生诊断会用可读语言说明最终选中的音轨、语言/default 信息和候选 margin。

### 阶段 3–5 完成：智能范围、大批次语义和视觉辅助

- 新增 React 无关的 `smartBatchPairing` 领域模块，支持 `S03E01`、`S03E01-E04`、`第三季1-4`、中文数字、项目 episodeKey 和 Emby 季集元数据。
- 只有当每个原片都能唯一归入一个参考范围时才缩小组合；任何缺失、重叠或冲突都会安全回退完整笛卡尔积。
- 真实 2×8 文件名形状被规划为 8 个显式关系并跳过 8 个跨分组组合；3×12 被规划为 12 组而非 36 组。
- 界面在启动前解释智能范围和节省组合数，并提供“分析全部组合”开关；文件名先验不会直接确认关系。
- 未找到共同内容现在显示为“没有对应关系（计算正常）”，不再与运行环境失败混为一谈。
- 精候选默认启用视觉证据辅助验证；视觉证据不能单独绕过音频候选和人工复核门控。
- 领域与匹配页专项测试共 52 项通过；ESLint 和音轨 Rust 专项测试通过。

### 阶段 6 完成：真实 2×8、候选保留与发布

- 真实 `2×8` 按文件名先验正确缩小为两个 `1×4` 智能分组，8 个关系均在 release 优化构建中真实执行完成，`failedPairCount=0`；不再把 8 个已知跨组无关系组合运行成“失败任务”。
- 第一组 E01–E04 用时约 250 秒，第二组 E05–E08 用时约 290 秒；两组都为每一集保留了完整可复核 TimeMap。
- 修复全局物理区间选择冲突后的候选丢失：即使批次安全门不允许自动采用，已完成精评估的最佳分段 TimeMap 仍会作为 `blocked` 候选交给人工复核，不再退化成“没有可查看结果”。
- 修复双音轨复核候选排序：用户显式选择优先；其次是已知语言一致；再考虑未知语言与默认轨；已知语言冲突不会仅凭覆盖率压过正确音轨。
- E02 最终自动回归选择 `#1 German [ger] [default]`，P95 216 ms、35 个 span、9 个歧义 span；此前误选英语轨时为 P95 393 ms、196 个 span、26 个歧义 span。
- 第二组 E05–E08 均选择默认德语 #1；P95 分别为 1136、207、49、300 ms。所有真实结果仍为 `blocked`，因为存在独立留出、局部边界或版本差异歧义，必须人工复核后才能采用，未放宽导出安全门。
- 被忽略的真实媒体测试支持通过环境变量显式指定参考或目标音轨，便于以后复现多音轨边界；Debug 未优化构建过慢不代表应用运行失败，正式回归使用 release 优化构建。

## 阶段 6 完成

- 主启动包由阶段 5 的 780.04 kB 降到 492.00 kB，不再触发 500 kB 警告。
- 智能匹配、原生对齐桥接、工程基准和兼容单文件导出改为按需加载。
- XML、候选、分集和导出清单使用浏览器原生渲染隔离，降低大列表离屏布局成本。
- 传统单集/分 P 导出仍有旧项目与 E2E 依赖，未误删；统一收进“兼容导出方式（单集或旧项目）”。
- 完成未使用旧 UI 审计：当前独立组件均有真实入口或自动测试契约，没有为满足清理数量而删除核心依赖。
- README 改为普通用户优先；新增文档导航、用户指南、故障排查、隐私、验证、更新记录和阶段 6 走查。
- 生成并人工检查 1280×720 素材页与校准页正式截图。
- 旧项目迁移、单 XML 兼容导出、多素材北极星流程、导出阻断和桌面分集导出安全门全部保留。

## 阶段 6 后 1×N 智能匹配热修

- 真实 1×5 失败日志确认：五组素材均已完成媒体探测与解码，最终失败发生在批次证据合同校验，不是视频损坏或音轨无法读取。
- 修复全局物理区间冲突分支：已解析但未入选的关系不再发布缺少执行证据的候选，也不再连带作废同批其他安全结果。
- 原生失败终态现在始终返回可校验的失败回执；若证据合同仍拒绝结果，技术诊断会保留准确拒绝原因。
- 前端不再用“原生任务清理状态不确定”覆盖最初的读取或合同错误；主错误与次级清理错误会同时保留。
- 第二次真实 1×5 日志进一步定位到 `fine frontier inventory member` 的 rank 顺序合同：算法按质量执行 `[2,1]`，旧代码却直接按该顺序封装证据，而校验器要求 canonical `[1,2]`。
- inventory receipt 与 fine execution receipt 现在共用同一 rank 规范化函数；只排序证据集合，不改变质量优先的实际执行与候选选择。
- 新回归固定覆盖“执行 `[2,1]`、证据 `[1,2]`”的真实故障形状，并让生成后的 inventory 通过同一严格合同。
- 合同校验错误已拆分为准确的 rank/字段原因；用户界面会明确说明属于应用内部校验，不再误导用户检查素材、FFmpeg 或 GPU。
- 自动测试、静态检查、端到端流程和 Windows release 构建均通过。仍需用新安装包重新运行该 1×5 批次，确认每一集候选质量后再采用关系。

## 最终验证

- `corepack pnpm verify:release`：通过。
- 源码审计：未发现待办标记或 TypeScript 裸 `any`。
- ESLint：通过。
- Vitest：98 个测试文件、997 项通过；1 项真实媒体环境测试按设计跳过。
- Rust：448 项通过、24 项按设计忽略；`cargo fmt --check` 与严格 Clippy 通过。
- Playwright：10/10 通过；包含旧项目打开、核心编辑、四步易用性和多素材北极星流程。
- 一万条 XML：导入后 382 个 DOM 节点；本次同机记录导入 276.4 ms。
- TypeScript/Vite production build：通过；主包 492.00 kB（gzip 157.68 kB）。
- Tauri release 与 NSIS 安装包：通过。

产物：

- `src-tauri/target/release/danmaku_timeline_studio.exe`
- `src-tauri/target/release/bundle/nsis/Danmaku Timeline Studio_0.1.0_x64-setup.exe`

最终可执行文件：16.16 MiB，生成时间 2026-07-20 13:07:55 +08:00，SHA-256 `405559D579A725DD932448C51B63EDAC450EE7C941188070F5C39BBE94089C9D`。

最终安装包：4.16 MiB，生成时间 2026-07-20 13:07:55 +08:00，SHA-256 `384FE2DF4B390E406137BC6C15666B78F93007313BAB5D968ED92B28BACD4EBB`。

## 已知边界

- 尚无获授权独立冻结 Gold 能证明最终同步 P95 或固定准确率；C137 不能标记为精度完成。
- 正式外部真实用户观察尚未执行；阶段 6 已提供观察脚本和完整自动/视觉走查，下一轮应邀请首次使用者完成。
- 当前安装包未做受信代码签名，首次安装可能出现 Windows SmartScreen。
- HTML 预览主要支持 MP4/WebM；MKV 预览需要桌面端配置 mpv。

## 下一步

1. 用一位未参与开发的用户按 `docs/USABILITY_REVIEW.md` 完成首次任务观察。
2. 在应用内逐集复核本次真实 2×8 的 blocked TimeMap，确认版本删减边界后再采用并导出。
3. 根据观察结果只修正用户卡点，不把工程验收术语重新放回默认界面。
4. 准备版本号、代码签名和公开分发说明。

## 历史

旧的长篇进度、C134–C137 计划和每阶段实现可从 Git 提交与标签查看；命令见 `docs/DEVELOPMENT.md`。2026-07-20 之前的完整上下文保存在提交 `d6020af`。
