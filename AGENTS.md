# AGENTS.md

## 项目初衷与产品边界

- 项目源于用户希望保留带弹幕观影的氛围，同时观看 Emby 中更高清、更完整的原片资源；B 站和用户上传视频主要被视为弹幕来源，而不是最终观看来源。
- B 站弹幕 XML 与 B 站视频时间轴强关联；本项目的核心任务是把这条参考时间轴上的弹幕，修正并投影到原片的标准时间轴上。
- B 站视频或用户上传素材只作为识别时间轴关系的证据：可能包含多集合并、前后无意义片段、审核删减、空白和局部偏移；这些关系必须表达为虚拟内容段、同步锚点、版本差异和映射规则。
- 项目不做视频剪切、分割、转码或重封装；视频处理属于剪辑软件职责。本项目的操作对象和目标输出始终是弹幕 XML、项目编辑状态和可复核的时间映射。
- 导出的修正弹幕应服务于外部成熟弹幕播放器（例如 Windows 端 Hills Lite 等）与 Emby 原片一起观看。

## UX 宪法

- `docs/ux-charter.md` 是所有 UI、交互和功能归置决策的最高依据；任何涉及用户可见界面的改动，先阅读并遵守它。
- 应用顶层动线固定为四页：素材 → 匹配 → 编辑 → 导出；任何功能必须归属四页之一。
- 每个涉及 UI 的阶段完成后，必须按北极星场景做完整走查验收（从空项目到导出分集 XML）。

## 长期工程规则

- 不再使用旧的分叉包装称呼描述当前分支；该分支称为产品成熟度提升主线或成熟度提升阶段。
- 后续开发只使用主目录 `C:\Users\lzp\Documents\Codex\2026-07-03\xml-ass-bilibili-xml-p-b`；不再使用 worktree 分叉作为日常开发入口，回退统一依赖 Git 提交、分支和 `checkpoint/...` 标签。
- UI 文案使用简体中文。
- 任何用户可见功能都必须先从普通用户视角审视：用户为什么需要它、什么时候需要它、是否需要亲自管理、能否看懂入口和结果；需要展示时必须提供清晰引导、可理解文案和真实可用的操作。
- TypeScript 标识符和文件名使用英文。
- 禁止使用 `any`，除非有明确注释说明原因、边界和替代方案。
- 原始 XML 数据不可被直接修改。
- 所有编辑必须是非破坏性编辑，通过片段、偏移、禁用、单条调整和映射规则表达。
- 所有用户可见功能必须有真实实现，不允许假按钮。
- 修改核心逻辑后必须运行测试。
- 每次对话产生可运行代码、UI 或 Tauri 变更后，默认完成相关测试并重新打包 release 产物，方便直接安装/运行验证；若未打包，必须明确说明原因。
- 每个可验收阶段完成后，默认创建 Git 提交；关键里程碑创建 `checkpoint/...` 标签，保证能按版本回退。
- 时间统一在内部使用整数毫秒。
- 每完成一个阶段更新根目录 `progress.md`；这是唯一进度文档。
- XML 解析、时间映射、导出、项目序列化等领域逻辑不得依赖 React。
- 用户主动导入的本地文件是本 Demo 的唯一数据来源。
- 视频内容不嵌入项目文件；项目文件仅保存媒体引用和编辑状态。
- 导出前必须重新解析验证导出的 XML。
- 核心交互需要键盘可达，并提供清晰焦点样式。
