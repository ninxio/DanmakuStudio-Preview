# AGENTS.md

## 长期工程规则

- UI 文案使用简体中文。
- TypeScript 标识符和文件名使用英文。
- 禁止使用 `any`，除非有明确注释说明原因、边界和替代方案。
- 原始 XML 数据不可被直接修改。
- 所有编辑必须是非破坏性编辑，通过片段、偏移、禁用、单条调整和映射规则表达。
- 所有用户可见功能必须有真实实现，不允许假按钮。
- 修改核心逻辑后必须运行测试。
- 不复制 GPL 项目代码，不照搬 GPL 项目的类名、函数名或完整算法实现。
- 不实现视频下载、DRM 绕过、账号绕过、私有接口爬取或未授权媒体访问。
- 时间统一在内部使用整数毫秒。
- 每完成一个阶段更新根目录 `progress.md`；这是唯一进度文档。
- XML 解析、时间映射、导出、项目序列化等领域逻辑不得依赖 React。
- 用户主动导入的本地文件是本 Demo 的唯一数据来源。
- 视频内容不嵌入项目文件；项目文件仅保存媒体引用和编辑状态。
- 导出前必须重新解析验证导出的 XML。
- 核心交互需要键盘可达，并提供清晰焦点样式。
