{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window permissions for local project files, Emby metadata access, and native path selection.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
